target/
*.onnx
.env

# b132-2-continuation: аудиторские отчёты (audit/ — карта атаки поверх публичного
# репо, находка аудита 2026-09-17) и локальная память агентов. Артефакты живут
# вне git; сам gitignore-паттерн — не секрет, доклад аудита уже написан.
audit/
.workbuddy-ai/
