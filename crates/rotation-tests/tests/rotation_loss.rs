//! Потери при ротации: отдельный прогон на **одном** канале и отдельный — на **обоих**.
//!
//! Разделение прогонов задано в `design/05-roadmap.md` Phase 0: «Отдельно — прогон с потерей на
//! одном канале и прогон с потерей на обоих (фолбэк-путь)». Смешивать их в один тест нельзя:
//! у них разные ожидаемые исходы (деградация против провала ротации).
//!
//! Бюджеты — из `design/02-protocols.md` §4 (таблица окна морфа) и `§3.5` (окно дедупа):
//! `T_morph` = 2 × SRTT, клип [200 ms, 2 s]; дублирование `N ≤ 4096` записей **или** `T_morph` —
//! что раньше; исчерпание → `MorphFailed` → откат + quarantine `T_quar` = 5 мин.
//!
//! Тела — `todo!()` под `#[ignore]`.
//! **Покрытие ТЗ:** сценарий 6 — двумя отдельными прогонами, как требует `05-roadmap`:
//! `rotation_loss_on_one_channel_stays_within_duplicate_budget` (деградация) и
//! `rotation_loss_on_both_channels_falls_back_to_buffer_path` (фолбэк-путь / провал ротации).

/// **Сценарий 6a (ТЗ): потеря на одном канале — дубли укладываются в `T_morph` и `N ≤ 4096`.**
///
/// Источник: `02 §3.3` (дублирование `seq >= continuity_point` на оба канала), `§3.5` (окно),
/// `§3.7` (`T_ack`), `§4` (бюджет окна морфа), `§3.9` («Duplicate-окно при ротации ≈ 1 RTT
/// дублированного трафика — bounded»).
///
/// Assert'ы:
/// 1. Канал нового узла (N2) теряет записи во время резюма, канал старого (N1) — нет:
///    выживший канал обязан донести **все** записи, ни одна не потеряна.
/// 2. Дубли идут по `seq >= continuity_point` и отсекаются дедупом `(sid, seq)` на узле —
///    `continuity_point` от повторов не двигается (`§3.3`, `§3.5`).
/// 3. Объём дублирования ограничен бюджетом окна морфа: `N ≤ 4096` записей **или** `T_morph` =
///    2 × SRTT, клип [200 ms, 2 s] — что раньше (`§4`). Это тот же бюджет, потому что дубли
///    ротации и морфа обслуживаются одним механизмом (`§3.3` шаг 5, `§4`).
/// 4. Порядок дублированного трафика — ≈ 1 RTT, не «до бесконечности» (`§3.9`).
/// 5. `T_ack` = 2 × SRTT, клип [200 ms, 2 s] (`§3.7`) — таймер владеет FrameSession.
/// 6. Сессия не рвётся (`§3.8`), `seq` продолжается (`§1`).
#[test]
#[ignore = "контракт Phase 0: тело намеренно todo!() — тест начнёт проходить вместе с реализацией"]
fn rotation_loss_on_one_channel_stays_within_duplicate_budget() {
    // Мок байндинга N2 с настраиваемым процентом потерь (не 0 и не 100): проверяем именно
    // выживание через дублирование, а не «оба канала идеальны» (это сценарий 1).
    todo!("Phase 0: потеря на N2, выживший N1 несёт всё; бюджет ≤4096 записей или T_morph (02 §3.3/§3.5/§4)")
}

/// **Сценарий 6b (ТЗ): потеря на обоих каналах — фолбэк-путь с буфером frame-слоя.**
///
/// Источник: `02 §3.7` (исчерпание/таймаут → откат), `§4` (`MorphFailed` → rollback +
/// quarantine), `05-roadmap` Phase 0 (прогон с потерей на обоих → фолбэк-путь) и таблица рисков
/// (буфер ≤ 16 МБ или ≤ 5 с — граница деградации).
///
/// Assert'ы:
/// 1. Оба канала теряют записи в окне ротации: **сразу** это не провал — включается буфер
///    frame-слоя (`05-roadmap`, риск: «ротация = graceful reconnect с буфером frame-слоя
///    (деградация, не провал)»).
/// 2. Пока буфер укладывается в **≤ 16 МБ или ≤ 5 с** — сессия жива, записи доставляются после
///    восстановления канала; **превышение — провал ротации, а не деградация** (`05-roadmap`,
///    это принятая граница; тест обязан её проверять, а не сглаживать).
/// 3. Исчерпание бюджета окна (`N ≤ 4096` или `T_morph`) даёт `MorphFailed` → ребро отката на
///    предыдущий байндинг, канал в quarantine `T_quar` = 5 мин (`§4`).
/// 4. `RESUME_NAK`/таймаут не считаются разрывом Aether-сессии: `K_session`, `stream_table` и
///    `seq` сохраняются, пока держится буфер (`§1`, `§3.8`).
/// 5. Измеряется frame-слой; разрыв прикладных TCP/QUIC сменой source IP — ожидаемый эффект
///    Phase 0, а не регресс (`05-roadmap`, граница теста).
#[test]
#[ignore = "контракт Phase 0: тело намеренно todo!() — тест начнёт проходить вместе с реализацией"]
fn rotation_loss_on_both_channels_falls_back_to_buffer_path() {
    // Два подпрогона: (а) буфер в бюджете → сессия жива после восстановления канала;
    // (б) буфер вне бюджета (>16 МБ или >5 с) → тест фиксирует провал ротации как отдельный
    // исход, а не маскирует его «деградацией».
    todo!("Phase 0: потеря на обоих каналах → буфер ≤16 МБ/≤5 с vs провал ротации; MorphFailed → quarantine (05-roadmap, 02 §4)")
}