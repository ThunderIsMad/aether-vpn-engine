[package]
name = "crypto-core"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
# Пин-версии — DEPENDENCIES.md → «Phase 0 pins».
# Смешение бэкендов (PQClean + RustCrypto) объяснено в QUESTIONS.md Q8: набор фич clatter
# объявлен явно; прод-бэкенд KEM — PQClean через clatter. RustCrypto `ml-kem` нужен только
# KAT/interop-тесту — это dev-dependency (F-10, аудит 3), не прод-зависимость.
clatter.workspace = true
chacha20poly1305.workspace = true
x25519-dalek.workspace = true
ed25519-dalek.workspace = true
# Вывод ключей: K_session / K_resume / re-key / ratchet записей (02 §1, §3.3, §5).
hkdf.workspace = true
hmac.workspace = true
sha2.workspace = true

[dev-dependencies]
ml-kem.workspace = true
